using UnityEngine;

/// <summary>
/// Автонастройка графики для мобильных устройств.
/// • Первый запуск определяет уровень качества по “железу” и сохраняет его в PlayerPrefs.
/// • Поддерживает 5 уровней: Very Low, Low, Medium, High, Very High (как в Project Settings → Quality).
/// • При последующих запусках берёт сохранённое значение без повторного расчёта.
/// </summary>
public class AutoGraphicsSettings : MonoBehaviour
{
  
}
